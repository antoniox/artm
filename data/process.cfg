corpus-directory = corpora
vocabulary-directory = vocabularies
model-directory = models
corpus = corpus
vocabulary = vocabulary
model = model
input-suffix = .shrunk
suffix = .shrunk
topics-count = 100
inner-iterations = 10
batch-size = 1000
